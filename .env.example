# 이 파일을 .env 로 복사한 뒤 키를 채우세요:  cp .env.example .env
# .env 는 .gitignore 에 있어 커밋되지 않습니다 (시크릿 전용).
#
# 필요한 건 OpenRouter API 키 하나뿐입니다. https://openrouter.ai/keys 에서 발급.
# 모델·딜레이 같은 튜닝값은 여기 두지 않습니다 —
# 톡방별 rooms/<톡방>/config.env (메뉴에서 관리) 또는 실행 시 CLI 인수로 지정하세요.
OPENROUTER_API_KEY=sk-or-...
